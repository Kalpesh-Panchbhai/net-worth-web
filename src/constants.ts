// Currencies supported for income entry and the display-currency preference.
// Keep in sync with the backend's accepted currency codes.
export const CURRENCIES = ["INR", "USD", "EUR", "GBP"] as const;

export const DEFAULT_CURRENCY = "INR";
